apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-active

spec:
  type: NodePort

  selector:
    app: {{ .Release.Name }}

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
